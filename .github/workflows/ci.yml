name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '15.4'

      - name: Build
        run: |
          xcodebuild -project Elapsed.xcodeproj \
            -scheme Elapsed \
            -configuration Debug \
            build

      - name: Test
        run: |
          xcodebuild -project Elapsed.xcodeproj \
            -scheme Elapsed \
            test
